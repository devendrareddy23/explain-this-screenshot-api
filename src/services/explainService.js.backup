exports.generateExplanation = async (imageUrl) => {
  return {
    explanation: `This screenshot appears to be located at ${imageUrl}. It likely contains visual information that would normally be analyzed by an AI vision model.`,
  };
};
